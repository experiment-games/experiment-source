Include( "shared.lua" )

function SWEP:DrawLargeWeaponBox( bSelected, xpos, ypos, boxWide, boxTall, selectedColor, alpha, number )
end

function SWEP:Draw( flags )
end

function SWEP:MuzzleFlash( pos1, angles, type, firstPerson )
end
